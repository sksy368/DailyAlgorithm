package Apr_2022.week3;

public class Solution_Programmers_59413_입양시각구하기 {
	/*
	 SET @hour := -1;
	  
	 SELECT @hour := @hour+1 as 'HOUR', (SELECT(COUNT(*) FROM ANIMAL_OUTS WHERE @hour=HOUR(DATETIME)) as 'COUNT'
	 FROM ANIMAL_OUTS
	 WHERE @hour < 23;
	 */
}
